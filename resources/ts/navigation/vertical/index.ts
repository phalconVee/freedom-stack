/**
 * FreedomStack: only user-journey navigation (FREEDOMSTACK_USER_JOURNEY.md §5).
 * Template demo sections (Apps, UI, Forms, Charts, Others) removed.
 */
import dashboard from './dashboard'
import type { VerticalNavItems } from '@layouts/types'

export default [...dashboard] as VerticalNavItems
